services:
  app:
    build: .
    ports:
      - "3001:3001"
    environment:
      - NODE_ENV=production
    volumes:
      - workout-data:/app/data
    restart: unless-stopped

volumes:
  workout-data:
